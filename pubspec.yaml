name: recurrence_kit

description: A standalone recurrence rule system with data models, computation engine, and picker UI widget.

version: 0.1.0

homepage: https://github.com/resengi/recurrence_kit
repository: https://github.com/resengi/recurrence_kit
issue_tracker: https://github.com/resengi/recurrence_kit/issues

environment:
  sdk: ^3.10.1
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0